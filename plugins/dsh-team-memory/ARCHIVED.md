# dsh-team-memory 已并入 dsh-hippo（2026-08-22 组合式决策）

核心代码在 hippo-skills 引擎 `src/team/`（账本/蒸馏/分诊/导出/循环）；
dsh 侧路由由 **dsh-hippo** 统一承载（`/dsh-team-memory/*` 路径保持兼容）；
GUI 面板在 hippo 工作台 `/team` 页。

本目录仅保留 PLAN.md（里程碑存档）。