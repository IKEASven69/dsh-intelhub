# 市场快照 · 2026-09-05（安全赛道单日涌入五工具）

> 对 awesome-dsh 9-05 全量快照（3196 个，一周 +28%）+ 五个 9-05 新入库安全插件的源码核查。

## 新入库的直接/相邻竞品（全部 9-05 一天入库）

| 插件 | 做什么 | 对 trust-list 的威胁 |
|---|---|---|
| **dsh-mcpguard**（ChenLaoshiYF） | 10 条规则：UNI 隐形字符 / B64 长串 / INJ×3（覆盖/角色扮演/多语言）/ PTH 敏感路径 / SHL 危险 shell / HMG 同形字（**含 U+1D400 数学字母区，我们当时没有**）；Python/Go/TS 三语言同规则；带 runtime observer | **正撞 v0.5 提示注入赛道**。但 642 行无真实语料、无 FP 实测、不区分模型面文件 |
| dsh-plugin-sentinel（BotonJ） | 装前静态审计：生命周期脚本/动态执行/凭据外传组合特征 + patch 层；零依赖、tar 全程不落盘 | 挤已拥挤的装前审计赛道，工程声明（不落盘）值得借鉴 |
| dsh-sentinel-scanner（Eligahyu） | 只读静态审计，0-100 风险分 | 同上；评分制 UX 可借鉴 |
| dsh-provenance（Darren-Tang） | 装前校验：待装包与读到的源码是否一致 | 与哈希锁互补（装前 vs 装后），暂不冲突 |
| dsh-plugin-scorecard（863683348） | 生态体检评分卡：0-100 分、A-D 等级、安全一票否决 | 质量评分视角，可借鉴呈现 |

## 对照后的护城河盘点

**被侵蚀**：「SKILL.md 内容注入检测」从无人做 → mcpguard 已进入，同形字覆盖一度反超（数学字母区）。
**仍然独占**：① 语料 + 量化质量声明（560 真实技能 0 误报 / TTP 9/9 / CI 周回归——五家新工具全没有）；② 依赖侧（install 脚本 / 版本锁 / 四家放行写回 / pnpm 工作流整合）；③ 集成度（四维 roster + 哈希锁 + 自动值守，单点扫描器无一对齐）。

## 当日已落地的对标补齐

- normalizeText 补数学字母数字区（U+1D400–1D7D6 十一个字块大小写 + 等宽数字）——修复 mcpguard 已用而我们缺失的混淆通道；修了无 `u` 旗标时星面 `\u{...}` 区间失效的 bug。
- 回归用例：数学无衬线 𝗌𝗂𝗅𝖾𝗇𝗍𝗅𝗒 混淆样本命中 via=normalized；ASCII 不受影响。

## 战略含义

检测规则本身正在商品化（一天五家），**能防守的只剩语料方法学和工作流整合**。下一步最有杀伤力的动作：拿我们的语料给五个竞品做一轮**横向误报/漏报实测**，发布对比报告——用数据说话，把"谁真正测得准"变成可验证的问题。
